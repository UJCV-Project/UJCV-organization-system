services:
  api:
    build: .
    ports:
      - "17504:17504"
    environment:
      - DATABASE_URL=postgresql://postgres:example@postgres:5433/classes?schema=public
    depends_on:
      - postgres
    volumes:
      - .:/app
      - /app/node_modules
    command: >
      sh -c "
        npx prisma generate &&
        npx prisma migrate deploy &&
        node dist/main.js
      "

  postgres:
    image: postgres:15
    restart: always
    ports:
      - "5433:5432" 
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
      POSTGRES_DB: classes
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
